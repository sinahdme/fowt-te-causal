# Causal Effect on FOWT via Transfer Entropy — Python deps
# Python 3.11 recommended (idtxl wheels available; openfast_toolbox tested).
# JDK 11 also required externally for IDTxl's JIDT backend.
# Install once response-channel list is locked; do NOT install yet.

# --- core analysis ---
numpy>=1.26,<2.1
scipy>=1.11
pandas>=2.1
pyarrow>=15.0           # parquet IO

# --- transfer entropy + mutual information ---
idtxl>=1.5              # KSG TE/MI, surrogate significance
jpype1>=1.5             # JIDT backend bridge for IDTxl

# --- sensitivity ---
SALib>=1.5              # Sobol / Morris

# --- OpenFAST output parsing + templating ---
openfast_toolbox>=4.0   # FASTOutputFile reader (.out / .outb)
jinja2>=3.1             # input-deck templating

# --- graph + plotting + reporting ---
networkx>=3.2
matplotlib>=3.8
seaborn>=0.13

# --- diagnostics / preprocessing ---
statsmodels>=0.14       # ADF stationarity test, ARIMA baselines
scikit-learn>=1.4
tqdm>=4.66

# --- dev / notebooks (optional but recommended) ---
jupyterlab>=4.0
ipykernel>=6.29
